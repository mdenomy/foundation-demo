<div class="row">
  <div class="small-12 medium-6 large-8 columns">
    <h1>Staff Picks</h1>
    <div class="row">
      <ul class="small-block-grid-1 medium-block-grid-1 large-block-grid-2">
        <% @reviews.each do |review| %>
          <% wine = review.wine %>
          <li class="staff-pick">
            <div class="outer">
              <span class="year"><%= wine.year %></span>
              <span class="price"><%= number_to_currency(wine.price, :precision => 2) %></span>
              <span class="producer"><%= wine.producer %></span>
              <span class="brand"><%= wine.brand %></span>
              <div class="wine-label"><%= image_tag(wine.image,  width: "100%") %></div>
              <span class="notes"><%= review.notes%></span>
            <div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="small-12 medium-6 large-4 columns">
    <div class="events">
      <h1>Events</h1>
      <ul>
        <% @events.each do |event| %>
          <li class="event">
            <span class="date"><%= event.date.strftime("%m/%d/%Y")%></span>
            <span class="title"><%= event.title%></span>
            <span class="description"><%= event.description%></span>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>


